{
  "name": "smart-inventory-system",
  "private": true,
  "packageManager": "pnpm@10.11.0",
  "workspaces": [
    "apps/web",
    "apps/server"
  ],
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "dependencies": {
    "@prisma/client": "^6.17.1"
  },
  "devDependencies": {
    "prisma": "^6.17.1"
  }
}
